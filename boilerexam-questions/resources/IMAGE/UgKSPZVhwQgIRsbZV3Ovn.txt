The image consists of two parts: a set of two equations on the left and a pole-zero plot on the right.

### 1. Equations (Text and Mathematical Symbols)
*   Top: `ω_m = 100 [rad/s]`
*   Bottom: `H_m = 10 [A/V]`

### 2. Pole-Zero Plot (Right Side)
*   **Axes:**
    *   Horizontal axis represents the real component, labeled with the symbol **σ** at the arrowhead on the right.
    *   Vertical axis represents the imaginary component, labeled with **jω** at the arrowhead at the top.
    *   The axes intersect at the origin (0,0).
*   **Geometry:**
    *   There are two 'X' markers (representing poles) plotted on the complex plane.
    *   Both 'X' markers are positioned at an identical negative real value on the σ-axis (the exact numeric value is not labeled, but they are clearly in the left-half plane).
    *   One 'X' is located in the second quadrant (positive jω, negative σ).
    *   One 'X' is located in the third quadrant (negative jω, negative σ).
    *   The two 'X' markers are vertically symmetric with respect to the horizontal σ-axis.