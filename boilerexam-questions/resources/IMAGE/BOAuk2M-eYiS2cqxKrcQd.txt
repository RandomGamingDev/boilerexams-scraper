The image displays a data table with two columns, enclosed in a double-lined border.

**Table Structure:**
*   **Header Row:** The columns are labeled "$n$" and "$T(n)$".
*   **Data Rows:** There are four rows containing numerical data pairs.

**Data Content:**
The values are organized as follows:

| Column 1 ($n$) | Column 2 ($T(n)$) |
| :--- | :--- |
| 40 | 15 |
| 80 | 60 |
| 160 | 240 |
| 320 | 960 |

**Layout Specifications:**
*   The table has a header row separated from the data rows by a horizontal line.
*   The entire table is encapsulated within double vertical lines on both the left and right sides.
*   Each row of data is separated by a horizontal line.