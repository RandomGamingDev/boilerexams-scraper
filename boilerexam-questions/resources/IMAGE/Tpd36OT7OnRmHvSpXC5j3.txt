This image is a Cartesian coordinate graph displaying two linear functions, $f(x)$ and $g(x)$.

### 1. Grid and Axes
*   **Grid:** A standard square grid is present.
*   **Horizontal Axis ($x$-axis):** A central horizontal line marks the $y=0$ baseline. The values "0.8", "1", and "1.2" are labeled along this axis.
    *   The spacing between 0.8 and 1.0 (and 1.0 and 1.2) is divided into two major grid squares, indicating each grid square horizontally represents 0.1 units.
*   **Vertical Axis ($y$-axis):** The central vertical line represents the $x=1$ position.
    *   Labels "-0.2" appear on the far left, aligning with horizontal grid lines. One "-0.2" is at the top, and another "-0.2" is near the bottom. 
    *   The distance between the $x$-axis and the labeled -0.2 grid line spans two vertical grid squares, indicating each grid square vertically represents 0.1 units.

### 2. Plotting of Functions
Both functions are depicted as straight lines that intersect at the point $(1, 0)$.

*   **Function $f(x)$ (Green line):**
    *   Passes through $(1, 0)$.
    *   At $x = 0.8$, the line passes through $y = -0.2$.
    *   At $x = 1.2$, the line passes through $y = 0.2$.
    *   The label "f(x)" is placed near the upper portion of the line.

*   **Function $g(x)$ (Blue line):**
    *   Passes through $(1, 0)$.
    *   At $x = 0.8$, the line passes through $y = -0.1$.
    *   At $x = 1.2$, the line passes through $y = 0.1$.
    *   The label "g(x)" is placed near the right side of the line.

### 3. Summary of Key Intersections
*   Both lines intersect at $(1, 0)$.
*   $f(x)$ has a steeper positive slope than $g(x)$.
*   The lines extend across the domain $x \in [0.8, 1.2]$.