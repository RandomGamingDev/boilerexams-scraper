This image contains a single text block structured as a pseudo-code algorithm within a rectangular border.

**Text Transcription:**
`algorithm BrunoSort(L):`
`    for (i = 0; i < 10; i = i + 1)`
`        // Use Selection Sort to sort the numbers based on the i-th digit.`
`    end for`
`end algorithm`

**Structural and Formatting Details:**
*   **Box:** The entire snippet is enclosed in a thin black rectangular frame.
*   **Indentation:**
    *   The `for` loop statement is indented one level relative to the `algorithm` header.
    *   The comment line (`// ...`) is indented two levels relative to the `algorithm` header.
    *   The `end for` statement is aligned with the `for` loop statement.
*   **Font:** Monospaced, black, sans-serif font.
*   **Symbols:**
    *   Parentheses `()` enclose the loop condition.
    *   Semicolons `;` act as delimiters within the loop header.
    *   The assignment and comparison symbols are standard (`=`, `<`, `+`).
    *   Double slashes `//` denote a comment.